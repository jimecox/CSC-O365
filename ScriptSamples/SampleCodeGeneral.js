
// compare two values on the screen, make sure both fields have a value
if ($("#sourceTask").val() != "" && $("#targetTask").val() != "") {

};

// make a GUID in javascript
var newId = SP.Guid.newGuid();

